export function toggleNotification (state, active) {
  state.active = active
}
export function setMessage (state, message) {
  state.message = message
}

export function setType (state, type) {
  state.type = type
}
